export interface CategoryItem {
    label: string;
    icon: JSX.Element;
    id: number;
}

export interface VideoLists {
    thumbnail: string;
    duration: string;
    channelImage: string;
    title: string;
    channel: string;
    views: string;
}